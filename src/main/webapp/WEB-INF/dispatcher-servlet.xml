<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc" xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
	http://www.springframework.org/schema/mvc
	http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- springmvc默认名为dispatchServlet.xml -->

    <!-- 扫描com.mmall包下，所有的controller注解 -->
    <context:component-scan base-package="com.mmall" annotation-config="true"/>

    <!-- 默认注解映射支持（必须要）：会为springmvc自动注册两个@controller分发请求所需的bean，并数据绑定支持、日期格式化支持、校验支持、json支持，
     也可以代替映射器、适配器-->
    <mvc:annotation-driven>
        <!-- 指定消息转换器，mvc:annotation-driven默认注册8个，指定后只注册你配置的 -->
        <mvc:message-converters>
            <!-- 数据与String类型的相互转换 -->
            <bean class="org.springframework.http.converter.StringHttpMessageConverter">
                <property name="supportedMediaTypes">
                    <list>
                        <value>text/plain;charset=UTF-8</value>
                        <value>text/html;charset=UTF-8</value>
                    </list>
                </property>
            </bean>
            <!-- springmvc 进行自动反序列化 json数据转换 -->
            <bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter">
                <property name="supportedMediaTypes">
                    <list>
                        <value>application/json;charset=UTF-8</value>
                    </list>
                </property>
            </bean>
        </mvc:message-converters>
    </mvc:annotation-driven>

    <!-- 文件上传 -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="10485760"/> <!-- 文件大小应小于10M -->
        <property name="maxInMemorySize" value="4096" /><!-- 内存给其分的空间大小 -->
        <property name="defaultEncoding" value="UTF-8"></property><!-- 默认编码风格 -->
    </bean>


</beans>
